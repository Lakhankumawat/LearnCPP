#include <iostream>

using namespace std;

int main()
{
    int width, length, result;
    cout << "Pleas enter Width and length:";
    cin >> width >> length;
    result = (width * 2) + (length * 2);
    cout << "Result: " << result;
}
